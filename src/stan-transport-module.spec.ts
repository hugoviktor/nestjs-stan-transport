import { StanTransportModule } from './stan-transport-module';

describe('StanTransportModule', () => {
  it('should be defined', () => {
    expect(new StanTransportModule()).toBeDefined();
  });
});
